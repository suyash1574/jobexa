{
  "endpoints": [
    {
      "path": "/api/v1/auth/gmail/authorize",
      "method": "GET",
      "summary": "Initiate Gmail OAuth 2.0 flow",
      "description": "Returns a redirect URL to Google's consent screen. Requires JWT user authentication.",
      "security": [{"BearerAuth": []}],
      "responses": {
        "307": {
          "description": "Redirect to Google OAuth consent page"
        },
        "401": {
          "description": "Unauthorized token"
        }
      }
    },
    {
      "path": "/api/v1/auth/gmail/callback",
      "method": "GET",
      "summary": "Google OAuth 2.0 redirect callback",
      "description": "Exchanges authorization code for access and refresh tokens, stores them securely in the database, and redirects back to dashboard.",
      "parameters": [
        {
          "name": "code",
          "in": "query",
          "required": true,
          "schema": {"type": "string"}
        },
        {
          "name": "state",
          "in": "query",
          "required": true,
          "schema": {"type": "string"}
        }
      ],
      "responses": {
        "302": {
          "description": "Redirects back to dashboard settings tab"
        },
        "400": {
          "description": "Invalid OAuth state or authorization code"
        }
      }
    }
  ]
}
